import { StyleSheet } from "react-native";

export const styleHeader = StyleSheet.create({
  container: {
    padding: 16,
    backgroundColor: "#FFF",
    alignItems: "center",
  },
  titulo: {
    paddingTop: 15,
    fontSize: 24,
    fontWeight: "bold",
    color: "#333",
  },
});
