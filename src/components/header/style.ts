import { StyleSheet } from "react-native";

export const styleHeader = StyleSheet.create({
  container: {
    backgroundColor: "#FFF",
    alignItems: "center",
  },
  titulo: {
    paddingTop: 30,
    fontSize: 24,
    fontWeight: "bold",
    color: "#333",
  },
});
